import 'package:flutter/material.dart';

class BackgroundBeams extends StatelessWidget {
  const BackgroundBeams({super.key});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
